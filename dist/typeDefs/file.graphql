scalar Upload

type Query {
  uploads: [File]
}
type Mutation {
  uploadFile(file: Upload!): File!
}
type File {
  id: ID!
  path: String!
  filename: String!
  mimetype: String!
  encoding: String!
}
